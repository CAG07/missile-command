# Missile Command Dependencies
# Based on original 1980 arcade game disassembly

# Core game engine
pygame>=2.5.0,<3.0.0

# Type hints (Python 3.8+ compatibility)
typing-extensions>=4.0.0; python_version < '3.10'

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Code quality
flake8>=6.0.0
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
